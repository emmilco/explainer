# explainer — session notes

Narrated-slideshow tool: agent-authored markdown decks render to self-contained reveal.js
slideshows with synced per-slide TTS. `PROJECT_INDEX.md` is the map — read it for structure,
the deck authoring format, the render pipeline, and the gpu-broker production rules.

## teach node

When the user says **"teach me about X"** (or "teach me more about X"), read
`teach/TEACH.md` and follow it. Learner profile: `teach/profile/profile.md`. Design:
`teach/DESIGN.md`.

Everything else proceeds as normal.

---

_This file mirrors `CLAUDE.md` — same rules for opencode and Claude Code; keep the two in sync._
