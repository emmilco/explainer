# Rendered output — regenerable from decks/ via render.py; large audio binaries.
# say audio re-renders instantly; Fish/Irons audio needs the GPU to reproduce.
out/

# Transient render scratch (GPU TTS job lists)
**/_tts_jobs.json
**/_fish_jobs.json

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Seeding corpus — personal claude.ai conversation analyses; local only
teach/seeding/
